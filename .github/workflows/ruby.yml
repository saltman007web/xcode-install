name: Ruby

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.0 # Specify the desired Ruby version
      - run: bundle install
      - run: bundle exec rake
